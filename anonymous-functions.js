// Anonymous functions - assigning a variable name. We store the reference to the function, but the function itself does not have a name.

let add = function (x, y) {
    return x + y;
}

console.log(add(5, 6))